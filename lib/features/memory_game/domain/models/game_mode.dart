/// Defines the available game modes for the memory game.
enum GameMode {
  /// Classic mode: player flips cards against a running timer.
  classic,

  /// Countdown mode: player must complete all matches before time expires.
  countdown,
}
