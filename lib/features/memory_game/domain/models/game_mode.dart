/// Defines the available game modes for the memory game.
enum GameMode {
  /// Player flips cards against a running timer.
  classic,

  /// Player must complete all matches before time expires.
  countdown,
}
